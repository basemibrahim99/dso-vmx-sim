venv/
__pycache__/
*.pyc
.DS_Store
logs/
plugins/
config/
.env

# Generated data -- fully reproducible via generate_data.py (fixed SEED=42),
# regenerated by the pipeline into Postgres, and re-exported by
# dashboard/export_dashboard_data.py. Not committed to keep the repo small
# and to keep "clone + run" as the source of truth instead of a stale export.
data/raw/
data/ground_truth/
dashboard/data/
.idea/
